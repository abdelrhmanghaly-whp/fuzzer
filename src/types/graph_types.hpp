#pragma once
#include<vector>
typedef long long ll;
// template<typename T>
// typedef std::vector<std::vector<T>> vvi;
typedef std::vector<ll> vll;
typedef std::pair<ll,int> pli;
const ll limit= 1e9;
const ll inf=2e18;
struct edge {
    int to,weight;
};